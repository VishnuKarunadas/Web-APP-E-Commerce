<%- include("../../views/partials/user/header") %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
<style>
  .btn {
      display: inline-block;
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      color: #fff;
      background-color: #000000;
      border: none;
      border-radius: 5px;
      transition: background-color 0.3s, transform 0.3s;
      cursor: pointer;
      text-align: center;
  }


  .btn:hover {
      background-color: #1e1d1d;
      transform: translateY(-2px);
  }


  .btn:active {
      transform: translateY(1px);
  }
  .wishlist-btn {
      position: absolute;
      top: 10px;
      right: 10px;
      background-color: rgba(255, 255, 255, 0.8);
      padding: 10px;
      border-radius: 50%;
      color: #000000;
      font-size: 18px;
      z-index: 10;
      transition: background-color 0.3s, color 0.3s, transform 0.3s;
  }


  .wishlist-btn:hover {
      background-color: rgba(255, 255, 255, 1);
      color: #1c1b1b;
      transform: translateY(-2px);
  }


  .wishlist-btn:active {
      transform: translateY(1px);
  }


  .product-img-action-wrap {
      position: relative;
  }

  .product-new-top {
  color: #ff0000; /* Red color for text */
  font-weight: bold; /* Make the text bold */
  padding: 0px 20px; /* Add padding around the text */
  background-color: #ffffff; /* White background for the button */
 
  border-radius: 5px; /* Rounded corners */
  text-transform: uppercase; /* Capitalize text */
  cursor: pointer; /* Pointer cursor on hover */
  transition: background-color 0.3s, color 0.3s; /* Smooth transition for hover effect */
}

.product-new-top:hover {
  background-color: #000000; /* Change background on hover */
  color: #ffffff; /* Change text color to white on hover */
}
.product-text{
  color: #000000; /* Red color for text */
  font-weight: bold; /* Make the text bold */

  /* background-color: #ffffff; White background for the button */
}
.product-text:hover {
 
  color: #000000; /* Change text color to white on hover */
}
  .product-img {
      position: relative;
      display: block;
      overflow: hidden;
  }


  .product-cart-wrap {
      border: 1px solid #eee;
      padding: 20px;
      margin-bottom: 30px;
  }


  .product-content-wrap h6 {
      font-size: 18px;
      color: #333;
      margin-bottom: 5px;
  }


  .product-content-wrap h5 {
      font-size: 16px;
      color: #ff6b6b;
      margin-bottom: 10px;
  }
  .nike {
  /* display: flex; */
  align-items: center;
  gap: 10px; /* Space between the buttons */
}

.nike .nike-cart {
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #f8f8f8; /* Background color for buttons */
  border: none;
  border-radius: 5px;
  padding: 10px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.nike .nike-cart i {
  color: #0c0c0c; /* Icon color */
  font-size: 18px;
}

.nike .nike-cart:hover {
  background-color: #ffffff; /* Change background on hover */
}


  .product-content-wrap a {
      display: inline-block;
      padding: 8px 12px;
      background-color: #ff6b6b;
      color: white;
      text-transform: uppercase;
      font-size: 14px;
      border-radius: 4px;
      text-decoration: none;
  }

/* Ensure the image is responsive and fits within the container */
img.img-fluid {
    width: 100%;      /* Makes the image width responsive */
    height: auto;     /* Maintain aspect ratio */
    max-width: 100%;  /* Prevent the image from overflowing */
    display: block;   /* Removes space below image */
    margin: 0 auto;   /* Centers the image horizontally */
}

/* Optional: set a specific size for product images if you want them to have a fixed size */
.men-thumb-item img {
    width: 350px;     /* Set a fixed width */
    height: 350px;    /* Set a fixed height */
    object-fit: cover; /* Makes sure the image is cropped to fit */
}

  .product-content-wrap a:hover {
      background-color: #000000;
  }
   /* Custom CSS to control the video size */
   .text_top{
    padding: 10px;
    text-align: center;
    font-size: 56px;
    color: #000000;
    
   }
   .small_text_top{
    font-size: 10px;
    text-align: center;
    padding-bottom: 10px;
   }

</style>


<div class="container-fluid">
  <div class="container-fluid">
    <!-- Custom Video Container with increased height -->
    <div class="custom-video-container row justify-content-center">
        <div class="col-12 col-md-12 col-lg-12">
            <video  muted autoplay loop class="w-100">
                <source src="/video/AIR MAX 97 __ Nike Ad (Unofficial) - Mudnag (1080p, h264, youtube).mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </div>
    </div>
    <div class="d-flex justify-content-center"></div>
    <h1 class="col-sl-6 col-md-8 col-lg-12 row justify-content-center text-center"> <strong>TOP OF THE WISHLIST KICKS</strong></h1>
    <h6 class="small_text_top">All-Time icons and sports-inspired gifts to make everyone on your list look and feel their best.</h6>
    <div class="d-flex justify-content-center">
      <a href="">
          <button type="submit" class="btn btn-black btn-lg" style="width: 120px; border-radius: 25px;">Shop Now</button>
      </a>
  </div>
    <br>
</div>


  <div class="inner-sec-shop px-lg-4 px-3">
    <div class="row">
      <% products.forEach(product => { %>
          <div class="col-md-3 product-men women_two mt-4">
              <div class="product-nike-info nike">
                  <div class="men-pro-items mt-2">
                      <div class="men-thumb-items mt-2">
                          <!-- Check if product.productImage exists and has at least one image -->
                          <% if (product.productImage && product.productImage.length > 0) { %>
                              <!-- Display the first image -->
                              <a href="/product/<%= product._id %>">
                                <img src="/uploads/product-images/<%= product.productImage[0] %>" class="img-fluid" alt="<%= product.productName %>">
                              </a>
                              
                          <% } else { %>
                              <!-- Fallback image if no image is available -->
                              <img src="/uploads/product-images/default.jpg" class="img-fluid" alt="<%= product.productName %>">
                          <% } %>
  
                          <div class="men-cart-pro">
                              <div class="inner-men-cart-pro">
                                  <a href="/product/<%= product._id %>" class="link-product-add-cart">Quick View</a>
                              </div>
                          </div>
                         
                      </div>
                      <div class="item-info-product">
                          <div class="info-product-price">
                              <div class="grid_meta">
                                  <div class="product_price">
                                      <h4>
                                          <a href="/product/<%= product._id %>" class="product-text">
                                               <%= product.productName %>
                                          </a>
                                      </h4>
                                      <script>
                                        document.addEventListener("DOMContentLoaded", function() {
                                            const maxLength = 20; // Set the maximum length for description truncation
                                    
                                            // Get all product descriptions
                                            const descriptionElements = document.querySelectorAll('.product-text');
                                    
                                            descriptionElements.forEach((descriptionElement) => {
                                                let description = descriptionElement.innerText || descriptionElement.textContent;
                                    
                                                // Truncate the description if it exceeds the max length
                                                if (description.length > maxLength) {
                                                    description = description.substring(0, maxLength) ;
                                                    descriptionElement.innerText = description;
                                                }
                                            });
                                        });
                                    </script>
                                      <div class="grid-price mt-2">
                                          <% if (product.offerPrice && product.offerPrice < product.salePrice) { %>
                                            
                                              <span class="money" style="color: rgb(0, 0, 0); font-weight: bold;">$<%= product.offerPrice %></span>
                                          <% } else if (product.salePrice < product.regularPrice) { %>
                                              
                                              <span class="money" style="color: rgb(0, 0, 0);">MRP: ₹<%= product.salePrice %></span>
                                          <% } else { %>
                                              <span class="money">$<%= product.regularPrice %></span>
                                          <% } %>
                                      </div>
                                  </div>
                                  <span class="money " style="color: green; padding-bottom: 15px;"><%= product.status %></span>
                              </div>
                              <ul class="stars">
                                  <% for (let i = 1; i <= 5; i++) { %>
                                      
                                          <a href="#">
                                              <% if (product.averageRating >= i) { %>
                                                  <i class="fa fa-star" aria-hidden="true"></i>
                                              <% } else { %>
                                                  <i class="fa fa-star-o" aria-hidden="true"></i>
                                              <% } %>
                                          </a>
                                      
                                  <% } %>
                              </ul>
  
                              <% if (product.totalRatings > 0) { %>
                                  <span class="rating-count">(<%= product.totalRatings %> reviews)</span>
                              <% } %>
                          </div>

                      </div>
                      <div class="clearfix"></div>
                  </div>
              </div>
          </div>
      <% }) %>
  </div>
  <br>
  <br>

  </div>
 
  <h3 class="tittle-nikelayout my-lg-4 my-4">Trending </h3>
  <div class="container-fluid">
    <div>
      <img src="/img/nike-just-do-it (5).jpeg"  class="col-12 col-md-12 col-lg-12 pt-2 pb-3">
    </div>
    <h1 class="col-sl-6 col-md-8 col-lg-12 row justify-content-center text-center"> <strong>VOMORO ROAM</strong></h1>
    <h6 class="small_text_top">Form meets function with the weatherised Vomero Roam—an all-season staple that delivers a comfortable stride and keeps you ready for whatever, no matter the weather.</h6>
    <div class="d-flex justify-content-center">
      <a href="/shop?searchQuery=women">
          <button type="submit" class="btn btn-black btn-lg" style="width: 120px; border-radius: 25px;">Shop Now</button>
      </a>
  </div>
        <h3 class="tittle-nikelayout my-lg-4 my-4">New Arrivals for you </h3>

      <div class="row">
        
      </div>

      <!-- Pagination -->
      <div class="pagination-container">
        <% if (currentPage > 1) { %>
          <a href="?page=<%= currentPage - 1 %>&sortBy=<%= sortBy %>">&laquo; Previous</a>
        <% } %> 
      
        <% for (let i = 1; i <= totalPages; i++) { %> 
          <% if (i === currentPage) { %>
            <span class="current-page"><%= i %></span>
          <% } else { %>
            <a href="?page=<%= i %>&sortBy=<%= sortBy %>"><%= i %></a>
          <% } %>
        <% } %> 
      
        <% if (currentPage < totalPages) { %>
          <a href="?page=<%= currentPage + 1 %>&sortBy=<%= sortBy %>">Next &raquo;</a>
        <% } %>
      </div>
      
  </div>
</div>

  
  
<%- include("../../views/partials/user/footer") %>

<script src="js/modernizr-2.6.2.min.js"></script>
<script src="js/classie-search.js"></script>
<script src="js/demo1-search.js"></script>
<script src="js/minicart.js"></script>
<script>
    $(document).ready(function () {
        $(".button-log a").click(function () {
            $(".overlay-login").fadeToggle(200);
            $(this).toggleClass('btn-open').toggleClass('btn-close');
        });
    });
    $('.overlay-close1').on('click', function () {
        $(".overlay-login").fadeToggle(200);
        $(".button-log a").toggleClass('btn-open').toggleClass('btn-close');
        open = false;
    });
</script>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
	
    function addToCart(productId) {
        const form = document.getElementById(`addToCartForm-${productId}`);
        const formData = new FormData(form);
      
        fetch("/add-cart", {
          method: "POST",
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(Object.fromEntries(formData)),
          credentials: 'include' 
        })
        .then(response => {
          return response.json().then(data => {
            if (!response.ok) {
              throw data;
            }
            return data;
          });
        })
        .then(data => {
          if (data.message === 'Item already in cart') {
            Swal.fire({
              icon: 'info',
              title: 'Already in Cart',
              text: 'This item is already in your cart.'
            });
          } else {
            Swal.fire({
              icon: 'success',
              title: 'Success',
              text: data.message
            });
          }
          if (data.cartItemsCount !== undefined) {
            updateCartItemCount(data.cartItemsCount);
          }
        })
        .catch(error => {
          console.error('Error:', error);
          if (error.error === 'Please login to add items to cart') {
            Swal.fire({
              icon: 'warning',
              title: 'Authentication Required',
              text: 'Please login to add items to cart',
              showCancelButton: true,
              confirmButtonText: 'Login',
              cancelButtonText: 'Cancel'
            }).then((result) => {
              if (result.isConfirmed) {
                window.location.href = '/login';
              }
            });
          } else {
            Swal.fire({
              icon: 'error',
              title: 'Error',
              text: error.error || 'An error occurred, please login and try again'
            });
          }
        });
      }
      
      function updateCartItemCount(count) {
        const cartCountElement = document.querySelector('.cart-count');
        if (cartCountElement) {
          if (count > 0) {
            cartCountElement.textContent = count;
            cartCountElement.style.display = 'inline';
          } else {
            cartCountElement.textContent = '';
            cartCountElement.style.display = 'none';
          }
        }
      }
      
      
      document.addEventListener('DOMContentLoaded', function() {
        const cartCountElement = document.querySelector('.cart-count');
        if (cartCountElement) {
          const initialCount = parseInt(cartCountElement.textContent, 10);
          updateCartItemCount(initialCount || 0);
        }
      });
    
      </script>















